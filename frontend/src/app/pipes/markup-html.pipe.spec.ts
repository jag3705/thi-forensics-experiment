import { MarkupHtmlPipe } from './markup-html.pipe';

describe('MarkupHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new MarkupHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
